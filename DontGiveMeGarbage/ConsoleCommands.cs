namespace DontGiveMeGarbage;

// ReSharper disable once UnusedMember.Global
public class ConsoleCommands
{
    //[ConsoleCommand("sophie.dgmg.istrash", "Checks whether the currently held item is tagged as coming from trash.")]
    //public static void IsTrash(string command, string[] args)
    //{
        //if (!Context.IsWorldReady)
        //{
            //Log.Error("This command should only be used in a loaded save.");
            //return;
        //}

        //if (Game1.player.CurrentItem is {modData: not null} && Game1.player.CurrentItem.modData.TryGetValue("sophie.DontGiveMeGarbage/ItemFromTrash", out string fromTrash))
        //{
            //if (bool.TryParse(fromTrash, out bool fromTrashBool) && fromTrashBool)
            //{
                //Log.Info("Currently held item is from trash");
            //}
            //else
            //{
                //Log.Info("Currently held item is not from trash");
            //}
        //}
    //}
}
